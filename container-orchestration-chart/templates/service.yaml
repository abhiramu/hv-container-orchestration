{{- range .Values.apps }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}-service
spec:
  selector:
    app: {{ .name }}
  ports:
    - port: {{ .service.port }}
      targetPort: {{ .containerPort }}
  type: {{ .service.type }}
---
{{- end }}